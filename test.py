#!/usr/bin/env python

code = __import__('clear-cache')

if __name__ == '__main__':
    code.contact_webmaster("test mail, don't worry")
    #code.is_available(debug = True)
    #code.clear_cache(debug = True)
